<h1>Listing bank Journals</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @journals.each do |journal| %>
  <tr>
    <td><%= journal.name %></td>
    <td><%= journal.description %></td>
    <td><%= link_to 'Show', bank_journal_path(journal) %></td>
    <td><%= link_to 'Edit', edit_bank_journal_path(journal) %></td>
    <td><%= link_to 'Destroy', bank_journal_path(journal), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bank Journal', new_bank_journal_path %>
